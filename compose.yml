name: "zx"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: "zigx.nuhu.dev"
    restart: unless-stopped
    networks:
      - npm

networks:
  npm:
    driver: bridge
